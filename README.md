# MIDAS TASKS by Avinoor Singh Kohli 2019460

## Installation
    $ virtualenv venv
    $ venv\Scripts\activate (for windows)
    $ source venv/bin/activate (for unix based systems)
    $ pip install -r requirements.txt
